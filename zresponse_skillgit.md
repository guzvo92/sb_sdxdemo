git add . && git commit -m $'[12_05_26] [01:22] clonando landing del prod, snapshot pipeline con vista progresiva y rename a satelldexdemo\n\n- landing marketing /: navbarhome fijo con drawer mobile custom sin Bootstrap, hero con terminal animator + token selector + metrics roller + community requests + alpha vip tester, statsbar tech stack hardcoded, hexnode con scan line animada e imagen Node.png, pillars HOLDER SCAN/LIQMAP/TIME SERIES, plans FREE/ALPHA con CTAs a /tracked y /global, banner hackathon hacia /hackathonview y /globalhackathon\n- dashboard previo movido de / a /tracked manteniendo sign gate ed25519, chips de tokens, 3 DistCards (Liquidity/Account/Tokens) con marine tiers WHALE/SHARK/DOLPHIN/FISH/CRAB/SHRIMP/PLANKTON, top 100 holders table y community gauge gamificado\n- snapshot pipeline fire-and-forget: boton REGEN admin-only en navbar dispara /api/regenerate que valida firma ed25519 + admin + anti-replay 300s, escribe snap_progress.json inicial y hace spawn detached "npx tsx makesnap.ts" + unref, devolviendo inmediato {started_at, targets_count, progress_url, poll_interval}\n- makesnap.ts en root: orquesta TODO secuencial por token (F1 DexScreener metadata + F2 Helius DAS getAsset + getTokenAccounts paginado hasta 20 pages, agrupa por owner, top 100 enriquecido con percentage_of_total_supply + value_today, bucketize en snapshot rico flat con an_perc_top10/20/50/100 + liq_/acc_/tok_ buckets), escribe holders/<slug>.json + snapshots/<slug>.json + dexscraptokens.json al final, persiste snap_progress.json en cada paso significativo\n- vista progresiva del REGEN: nuevo endpoint GET /api/snap_progress sin auth lee snap_progress.json, el navbar polea cada 1s y renderiza panel flotante bottom-right con header status+elapsed, token actual [N/T]+fase+step, barra de progreso, lista de completados con check/cross + slug(symbol) + holders count + elapsed\n- lib/scraper.ts client DexScreener (fetchDexScreenerToken + fetchAllTargets concurrencia 5), lib/helius.ts client Helius DAS (fetchTokenAsset + fetchTokenHolders paginado con onProgress + enrichHolders + buildSnapshotRich, tolera HELIUS_API_KEY como UUID crudo o URL completa de mainnet.helius-rpc.com), lib/verifySign.ts con verifyEd25519 + isAdmin + parseTimestampedMsg compartido por endpoints\n- targets.json en public/ root con 8 memes importados de la DB MySQL del prod sb_satelldex via JOIN Target/TokenCategoryAssign/TokenCategory: buck, fartcoin, giga, popcat, retardio, weed, usduc, hanta\n- CRUD community_requests file-based: endpoint /api/community_request (GET lista por votes desc + POST append con firma ed25519 anti-replay 300s, si CA repetido incrementa votos), form /requiretoken con campos token_address/symbol/name/price/fdv/pools y signMessage, sec6_communityreq fetcheando del endpoint en el hero\n- admins.json reubicado de public/demo-data/admins.json a public/admins.json (root de public/) para fetch directo /admins.json sin namespace demo-data, actualizando navbarhome y los Navbar inline de /tracked y /global\n- marine tiers en bar charts de /global (sec_yestmul_nwalls + nwallsper con BRACKET_META y prettyBracketLabel) y nuevo HUD tab CLUSTERS BY TOKEN con line chart por token (sec_token_nwallsclust) mostrando evolucion temporal de brackets habilitados\n- AppWalletProvider extraido de layout.tsx a app/components/ con Phantom + Solflare + SolanaMobileWalletAdapter explicitos, layout.tsx queda como server component\n- container Docker renombrado satelldexdemofront -> satelldexdemo: stop+rm del anterior y docker run con --name satelldexdemo --user 1000:1002 -v $PWD:/app -w /app -p 127.0.0.1:4950:3000 --restart unless-stopped, refleja que el proyecto es unificado (Next 16 sirve front + Route Handlers sin backend separado)\n- deps nuevas: tweetnacl ^1.0.3 + bs58 ^6.0.0 para verificacion ed25519 server-side, tsx ^4.19.2 en devDependencies para correr makesnap.ts via child_process.spawn (intentamos ts-node pero falla con ERR_UNKNOWN_FILE_EXTENSION en Node 20 para imports ESM, tsx maneja .ts ESM out-of-the-box)\n- env var nueva HELIUS_API_KEY requerida por F2 Helius, se carga desde .env.local (Next la inyecta al proceso parent, spawn la hereda al child de makesnap)\n- docs: README expandido con seccion Snapshot pipeline F1+F2 con shapes JSON de cada output, error codes separados por fase, ejecucion manual de makesnap desde host con HELIUS_API_KEY exportada o inline y flags --targets/--out/--top/--dry-run, comandos docker run para recrear el container con HELIUS_API_KEY via -e, nota del rename del container; summary.md v1.6 -> v2.1 con file layout completo (lib/, makesnap.ts root, snap_progress.json), rutas todas (/api/regenerate fire-and-forget + /api/snap_progress polling + /api/community_request), flujo end-to-end del REGEN, browser flow actualizado, autogeneracion bumpeada' && git push origin master
