git add . && git commit -m $'[12_05_26] [18:08] panel SNAPSHOTS AVAILABLE en /globalhackathon + dateFolder de makesnap pasa a UTC + cache TTL del snap_index\n\n- /globalhackathon UI: nuevo panel "◈ N SNAPSHOTS AVAILABLE" arriba de los HUD tabs, fetch a /api/snap_index en paralelo con catRes y histRes, state snapFolders, render como lista de chips monospace en flex-wrap, el ultimo folder resaltado con borde cyan + label "latest"\n- helper fmtFolder parsea YYYY_MM_DD__HH_MM (y formato viejo YYYY_MM_DD sin sufijo) a hora local del browser via Date.UTC + toLocaleString (dia/mes corto + HH:MM si hay sufijo de hora)\n- makesnap.dateFolder pasa de hora local del server a UTC (getUTCFullYear/Month/Date/Hours/Minutes): los nombres de folder son unicos globalmente sin importar la TZ del container, y el UI los convierte a hora local al renderizar\n- demoSdk.getIdx() cambia de cache singleton sin TTL a cache con TTL de 5s (IDX_TTL_MS): re-renders rapidos no spamean /api/snap_index pero un REGEN reciente aparece sin recargar la pagina\n- snapshots reales generados por runs nuevos de REGEN: folders 15_28, 15_29, 16_10 y 16_11 del 12_05 en holders/, snapshots/ y dexscraptokens/\n- summary v3.1 -> 3.2: file layout extendida con notas del TTL en demoSdk, panel SNAPSHOTS AVAILABLE en globalhackathon y chartjs.d.ts' && git push origin master
