git add . && git commit -m $'[12_05_26] [01:47] limpiando data dummy, agregando layout por fecha sin sufijos y endpoint snap_index para auto-detect del folder mas reciente\n\n- limpieza data dummy: borrados los 6 holders/<slug>.json y 6 snapshots/<slug>.json de los slugs alphax/axiomai/byteflow/metridot/novacoin/zorbax que arrastraba el seed inicial. tokens.json regenerado desde targets.json + dexscraptokens + snapshots con los 8 memes reales (buck/fartcoin/giga/popcat/retardio/weed/usduc/hanta). global/categories.json reescrito a una sola categoria "Memes" con los 8 slugs. global/global_history.json reescrito con el snapshot real de cada uno (perc_*, nholders_*, acc_/tok_ buckets)\n- layout final por fecha sin sufijos: makesnap.ts escribe solo en holders/<YYYY_MM_DD>/<slug>.json + snapshots/<YYYY_MM_DD>/<slug>.json + dexscraptokens/<YYYY_MM_DD>/all.json. Sin LATEST sueltos (snapshots/<slug>.json directo eliminado), sin sufijo __<runts> (cada run del mismo dia sobreescribe el archivo del dia). Helper dateFolder() construye YYYY_MM_DD desde la fecha local del server. Migracion de archivos existentes hecha a mano: rm de LATEST sueltos + rename de <slug>__<runts>.json a <slug>.json + dexscraptokens history como all.json\n- endpoint /api/snap_index: GET sin auth lista las subcarpetas YYYY_MM_DD validas en snapshots/, holders/ y dexscraptokens/, devuelve {ok, snapshots:{folders[],latest}, holders:{folders[],latest}, dexscraptokens:{folders[],latest}}. Cada seccion del front lo consulta para construir los paths de fetch contra <latest>/<slug>.json sin hardcodear la fecha\n- front adapta a snap_index: app/tracked/page.tsx fetchea /api/snap_index al firmar y guarda snapIdx={snapshots,holders}, construye los fetch de snapshots y holders contra el latest folder. app/home/comp_tokenselector.tsx hace lo mismo para dexscraptokens (lee dexscraptokens.latest/all.json en lugar del LATEST suelto)\n- doble persistencia anterior (LATEST + HISTORY con sufijo runts) reemplazada por persistencia simple por dia. Trade-off: se pierde time-series intra-dia (multiples runs del mismo dia se sobreescriben) a cambio de paths limpios y descubrimiento explicito via endpoint\n- comp_tokenselector refactor a targets.json: lee public/targets.json (lista canonica de slugs) + dexscraptokens del latest folder (symbols scrapeados). Renderiza una sola seccion "Memes" con los 8 slugs reales. Chip muestra symbol uppercase scrapeado + slug en gris como subtitulo\n- vista progresiva REGEN: endpoint /api/regenerate fire-and-forget (spawn detached + unref) devuelve inmediato {started_at, targets_count, progress_url, poll_interval}, endpoint GET /api/snap_progress sin auth lee snap_progress.json que makesnap.ts escribe en cada paso. navbar polea cada 1s y renderiza panel flotante bottom-right con header status+elapsed, token actual [N/T]+fase+step, barra de progreso y lista de completados con check/cross + holders count + elapsed\n- makesnap.ts loadEnvLocal(): parser minimo de .env.local que se ejecuta al inicio del IIFE. Necesario porque docker exec lanza un proceso nuevo que NO hereda las env vars que Next inyecta (.env.local solo lo lee Next al arrancar). Solo setea vars que no estan ya en process.env (no sobrescribe el spawn desde el endpoint)\n- helius.ts extractKey(): tolera HELIUS_API_KEY como UUID crudo o como URL completa de mainnet.helius-rpc.com (extrae ?api-key=UUID via regex). Permite pegar la URL completa de la consola de Helius sin parsear manualmente\n- hero roller fuera del container 1280: Comp_MetricsRoller queda como hijo directo del section padre (width:100%), no del hero-container (maxWidth:1280px), para que la banda full-bleed llegue de borde a borde del viewport\n- runtime tsx en vez de ts-node: ts-node falla con ERR_UNKNOWN_FILE_EXTENSION en Node 20 para imports ESM aunque haya override compilerOptions.module=commonjs. tsx ^4.19.2 maneja .ts ESM out-of-the-box sin config\n- README expandido con secciones de layout por fecha (paths limpios sin sufijo, /api/snap_index para auto-detect), shapes de holders/<slug>.json (array de 100 enriquecidos), shapes de snapshots/<slug>.json (flat con an_perc_top* + liq_/acc_/tok_ buckets), ejecucion manual de makesnap desde host con HELIUS_API_KEY y flags --targets/--out/--top/--dry-run\n- summary.md bumped 2.1 -> 2.3 con file layout (lib/, makesnap.ts root, snap_progress.json), rutas (/api/regenerate fire-and-forget + /api/snap_progress polling + /api/snap_index + /api/community_request), flujo end-to-end del REGEN, browser flow, categorias actualizadas a Memes' && git push origin master
