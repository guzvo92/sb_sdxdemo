git add . && git commit -m $'[12_05_26] [09:23] fetch_globalrun_history del SDK reconstruye el agregado multi-token leyendo todos los folders de snapshot\n\n- demoSdk.fetch_globalrun_history dejo de leer el legacy global_history.json (que tenia N_SNAPS=1 hardcoded) y ahora lista folders via /api/snap_index, cruza con targets.json para filtrar slugs validos, y fetcha snapshots/<folder>/<slug>.json en paralelo (Promise.all sobre el producto cartesiano folders x slugs); agrupa por slug, ordena por runts asc y emite tokens[].timestamps[] solo con series no vacias\n- shape de cada timestamp mapeado al contrato que buildTimestamp consume en /globalhackathon: liq_* 1:1 a perc_* (bigpool/top1_10/top11_20/top21_50/top51_100/others), acc_<bucket> remapeado a nholders_<bucket> (over50000, 10000to50000, 5000to10000, 1000to5000, 500to1000, 100to500), nholders_under100 con fallback a acc_under100, perc_nholders_<bucket> calculados sobre nholders_full con helper pct() a 2 decimales\n- efecto en /globalhackathon: el tab CLUSTERS BY TOKEN ahora muestra todos los snapshots disponibles por slug (1 punto hoy con 1 folder, crece automaticamente con cada REGEN); el tab LAST SNAPSHOT toma el ultimo timestamp de cada serie\n- summary v3.0 -> 3.1: descripcion de demoSdk en file layout extendida con fetch_globalrun_history, nota de global_history.json marcado como LEGACY huerfano (no consumido, dejado en disco para no romper algo mas), TokenSelector en adaptaciones vs prod actualizado (lee dexscraptokens del folder mas reciente)' && git push origin master
